function Spinner() {
  return (
    <div
      style={{
        textAlign: "center",
        padding: "30px",
        fontSize: "20px",
        fontWeight: "bold",
      }}
    >
      Loading...
    </div>
  );
}

export default Spinner;